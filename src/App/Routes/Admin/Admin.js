import React from 'react';

import Layout from 'shared/components/Layouts/FormLayout';

const Admin = () => (
  <Layout>
    <h1>
      <span role="img" aria-label="an admin">
        ️🧓🏽
      </span>{' '}
      Admin As FFFFFFFFFF{' '}
      <span role="img" aria-label="an admin">
        ️🧓🏽
      </span>
    </h1>
  </Layout>
);

export default Admin;
