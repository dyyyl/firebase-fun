import styled from 'styled-components';

const Form = styled.form`
  display: flex;
  width: 35rem;
  flex-direction: column;

  h1 {
    margin-bottom: 3rem;
  }

  input {
    margin-top: 3rem;
  }

  button {
    margin-top: 2rem;
  }
`;

export default Form;
